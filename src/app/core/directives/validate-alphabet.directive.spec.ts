import { ValidateAlphabetDirective } from './validate-alphabet.directive';

describe('ValidateAlphabetDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAlphabetDirective();
    expect(directive).toBeTruthy();
  });
});
